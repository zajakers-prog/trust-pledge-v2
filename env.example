# ====================================
# TrustPledge v2 - 환경변수 설정 가이드
# 이 파일을 복사해서 .env.local 로 저장하고 값을 채우세요.
# ====================================

# ----------------------------------
# Supabase (필수)
# https://supabase.com → 프로젝트 생성 → Settings → API
# ----------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ----------------------------------
# NextAuth (필수)
# NEXTAUTH_SECRET: openssl rand -base64 32 로 생성
# NEXTAUTH_URL: 로컬은 http://localhost:3000, 배포 후엔 실제 도메인
# ----------------------------------
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000

# ----------------------------------
# Google OAuth (필수)
# https://console.cloud.google.com → API & Services → Credentials
# ----------------------------------
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ----------------------------------
# Apple OAuth (선택 - 나중에 설정 가능)
# ----------------------------------
APPLE_ID=your-apple-id
APPLE_SECRET=your-apple-secret
